<% if @cocktail %>

  <h1>Search Result: <%= @cocktail[:name] %></h1>

  <img src="<%= @cocktail[:image_url] %>" alt="">

  <%= link_to "Add #{params[:query]} to your cocktails", cocktails_add_path(params[:query]) %>
<% else %>
  <h1>Oops, no valid result returned.</h1>
<% end %>
